                                     ------------------------------------GENERAL--------------------------------------


1.Identify cves and exploit
            
   => understand the tech stack and versions
		whatweb https://target.com
		wappalyzer https://target.com
		nmap -sV -Pn target.com
		nmap -sV -T4 licet.ac.in
		nuclei -l live_subdomains.txt -t technologies/

                This gives you:
		   Web servers: Apache, Nginx, IIS, etc.
		   Frameworks: Laravel, Struts, Express, etc.
		   CMS: WordPress, Joomla
		   JS Libraries: jQuery, React versions
		   Backend stack: PHP, ASP.NET, Java, etc.

		1. analyze httpx results from recon
		2. analyze res headers
		3. use wappalyzer

		=>note down the technoogies and version...
   
      =>search cves for identified tech stack

		Once you know the stack:
		1.google it eg: jiraa exolit ,jira cve..etc...

		2.Search CVEs by software/version:
                    https://www.cvedetails.com
                    https://vulmon.com
		    https://nvd.nist.gov
		    https://exploit-db.com

		    Use keywords like:
				apache 2.4.52
				wordpress 6.0
				php 8.1.1


2.look for admin pages and try default passwords

3.curl -X GET https://www.target.com | grep -i -E 'pass|pwd|passphrase|cred|encrypt|appkey|token|secret|auth|authorization|key|private'

4.take a look at archieved urls, 

eg: if https://target.com/getusers.php
       try  https://target.com/getadmin.php =>ipdi try panlaam
    
5.inurl:"/geoserver/ows?service=wfs" "target"
	nuclei -l urls_endpoints.txt -tags geoserver -severity critical,high,medium

6.intitle:dashboard-confluence "target" (#related CVE: CVE-2019-3396)
	ruby /home/maddy/techiee/bug_bounty/2_phase_recon_autom/automation/tools/cve-2019-3396.rb -t  https://licet.ac.in  --scan

7.using rust scanner: (inp:redirect_params.txt)
	cargo build
	cargo run
	refer: automation/tools/open

8.Sites:
	visit: https://cyscan.io
	 => can find other info,vul,s,deepscan,fuzzing,etc...
	 => copy subdomains,jsfiles,urls for furter recon
	 
	 visit: https://webscout.io for ip's,more..
	 
	 visit: virustotal
	 =>collect urls and sens info
	 
9.check sens endpoints by robots.txt,sitemap.xml
	 =>check normal url in web
	 =>likewise check webarchieve,it may contain sens endpoints
	 
10.xss testing,sql testing, hidden params on param urls

# echo "[*] Testing for SQLi using  sqlmap..."
# script -q -c " python3 /home/maddy/techiee/bug_bounty/2_phase_recon_autom/tools/sqlmap/sqlmap.py -m "$ALLPARAMS" --level 5 --risk 3 --batch --dbs --tamper=between --random-agent" "$OUTDIR/sqlmap_output.txt"


# echo "[*] Testing for XSS using dalfox..."
# dalfox file "$ALLPARAMS" --skip-bav --silence -o "$OUTDIR/dalfox_xss.txt"


echo "[*] Finding Hidden Parameters using Arjun..."
# while read url; do
#   echo "[$(date '+%T')] Scanning: $url" | tee -a "$OUTDIR/hidden_params.txt"
#   echo "==== URL: $url ====" >> "$OUTDIR/hidden_params.txt"
#   arjun -u "$url" -oT - >> "$OUTDIR/hidden_params.txt"
#   echo -e "\n" >>"$OUTDIR/hidden_params.txt"
#   sleep 2
# done < "$ALLPARAMS" 


11.sql ttest on login,other endpoints:
eg: python3 /home/maddy/techiee/bug_bounty/2_phase_recon_autom/tools/sqlmap/sqlmap.py -r request --dump --batch --dbs [ here, request=>raw http request burp la irudhu copy pnadhu ]
